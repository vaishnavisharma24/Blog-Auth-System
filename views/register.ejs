<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            background: linear-gradient(to right, #6a11cb, #2575fc);
            color: #fff;
            font-family: Arial, sans-serif;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .register-container {
            background: #fff;
            color: #333;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            padding: 30px;
            width: 100%;
            max-width: 400px;
        }
        .register-container h1 {
            text-align: center;
            margin-bottom: 20px;
            font-weight: bold;
            color: #6a11cb;
        }
        .form-control {
            border-radius: 30px;
        }
        .btn-primary {
            background: #6a11cb;
            border: none;
            border-radius: 30px;
            padding: 10px 20px;
            transition: background 0.3s;
        }
        .btn-primary:hover {
            background: #2575fc;
        }
        .form-control:focus {
            box-shadow: none;
            border-color: #6a11cb;
        }
        .alert-custom {
            color: #fe0a0a;
            padding: 3px;
            border-radius: 5px;
            font-size: 0.9rem;
            margin-bottom: 15px;
            text-align: center;
        }
        .alert-custom-success {
            color: #37ff00;
        }
        .hover-effect {
            color: #6a11cb;
            text-decoration: none;
            transition: color 0.3s;
        }
        .hover-effect:hover {
            color: #2575fc;
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="register-container">
        <h1>Register</h1>
        <!-- Flash Message -->
        <% if (typeof error_msg != 'undefined' || typeof success_msg != 'undefined') { %>
            <% if (typeof error_msg != 'undefined') { %>
                <div class="alert alert-custom"><%= error_msg %></div>
            <% } %>
            <% if (typeof success_msg != 'undefined') { %>
                <div class="alert alert-custom alert-custom-success"><%= success_msg %></div>
            <% } %>
        <% } %>

        <form action="/users/register" method="POST">
            <div class="mb-3">
                <label for="username" class="form-label">Username</label>
                <input type="text" name="username" id="username" class="form-control" value="<%= typeof username != 'undefined' ? username : '' %>" placeholder="Enter your username">
            </div>
            <div class="mb-3">
                <label for="email" class="form-label">Email</label>
                <input type="email" name="email" id="email" class="form-control" value="<%= typeof email != 'undefined' ? email : '' %>" placeholder="Enter your email">
            </div>
            <div class="mb-3">
                <label for="password" class="form-label">Password</label>
                <input type="password" name="password" id="password" class="form-control" placeholder="Enter your password">
            </div>
            <div class="mb-3">
                <label for="password2" class="form-label">Confirm Password</label>
                <input type="password" name="password2" id="password2" class="form-control" placeholder="Confirm your password">
            </div>
            <button type="submit" class="btn btn-primary w-100">Register</button>
        </form>
        <div class="text-center mt-3">
            <p>Already have an account? <a href="/users/login" class="hover-effect">Login</a></p>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
